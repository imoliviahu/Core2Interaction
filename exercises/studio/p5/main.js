function setup() {
    resizeCanvas(windowWidth, windowHeight);
    background("MediumSpringGreen");
}
  
function draw() {
    strokeWeight(2);
    fill(251,21,145, 30);
    ellipse(mouseX, mouseY, 250, 250);
}